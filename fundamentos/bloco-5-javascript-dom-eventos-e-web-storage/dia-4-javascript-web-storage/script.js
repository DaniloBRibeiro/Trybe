const body = document.querySelector('body');
const paragraph = document.querySelector('p');

const colorBg = body.style.backgroundColor
const colorText = paragraph.style.color
const fontSize = paragraph.style.fontSize
const fontSpace = paragraph.style.lineHeight
const fontFamily = paragraph.style.fontFamily


console.log(body.style.backgroundColor);
// localStorage.setItem('bgc', JSON.stringify(body.style.backgroundColor));